export function PrototypeNote() {
  return (
    <aside className="prototype-note" aria-label="Prototype content notice">
      <strong>Prototype content</strong>
      <span>
        Demo imagery and settings show the product experience only. They are not verified camera-output proof.
      </span>
    </aside>
  );
}
